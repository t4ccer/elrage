[package]
name = "agenix_ng_el"
version = "0.1.0"
edition = "2021"

[dependencies]
age = {version = "0.10.0", features = ["ssh"]}
emacs_module = {path = "../emacs_module"}

[lib]
path = "lib.rs"
crate-type = ["rlib", "cdylib"]
